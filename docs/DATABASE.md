# Database

The active database documentation is in [`../database/README.md`](../database/README.md).

Production uses Supabase PostgreSQL. Alembic migrations are stored under `database/migrations/` and are run separately from Vercel application startup.
