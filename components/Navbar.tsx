'use client'
import Link from 'next/link'
import {useEffect,useState} from 'react'
import {ArrowUpRight,Menu,X} from 'lucide-react'
const links=[['Residences','/residences'],['Amenities','/amenities'],['Experience','/experience'],['Smart Living','/smart-living'],['Location','/location'],['About','/about']]
export function Navbar(){const[open,setOpen]=useState(false);const[solid,setSolid]=useState(false);useEffect(()=>{const f=()=>setSolid(scrollY>80);f();addEventListener('scroll',f,{passive:true});return()=>removeEventListener('scroll',f)},[]);return <header className={`fixed inset-x-0 top-0 z-50 border-b transition-all duration-500 ${solid||open?'border-charcoal/10 bg-ivory/95 text-charcoal backdrop-blur-md':'border-ivory/20 text-ivory'}`}><div className="page-shell flex h-20 items-center justify-between"><Link href="/" className="font-display text-2xl leading-none">ONA <span className="block font-sans text-[8px] tracking-[.35em]">TOWERS</span></Link><nav className="hidden items-center gap-7 lg:flex">{links.map(([x,h])=><Link className="text-xs font-medium" href={h} key={h}>{x}</Link>)}</nav><div className="flex items-center gap-3"><Link href="/enquire" className={`hidden rounded-full px-5 py-2.5 text-xs font-medium md:flex items-center gap-2 ${solid||open?'bg-charcoal text-ivory':'bg-ivory text-charcoal'}`}>Enquire <ArrowUpRight size={14}/></Link><button onClick={()=>setOpen(!open)} className="lg:hidden" aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="page-shell flex flex-col gap-5 border-t border-charcoal/10 bg-ivory py-8 text-2xl font-display text-charcoal lg:hidden">{links.map(([x,h])=><Link href={h} key={h} onClick={()=>setOpen(false)}>{x}</Link>)}<Link href="/enquire" onClick={()=>setOpen(false)}>Enquire</Link></nav>}</header>}
