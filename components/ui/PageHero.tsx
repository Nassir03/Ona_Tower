import {Media} from './Media'
export function PageHero({eyebrow,title,copy,image='/placeholders/hero.svg'}:{eyebrow:string;title:string;copy:string;image?:string}){return <section className="relative min-h-[78svh] bg-charcoal text-bone"><Media src={image} alt="Architectural concept placeholder" priority className="absolute inset-0 h-full opacity-55"/><div className="absolute inset-0 bg-gradient-to-r from-charcoal/80 to-transparent"/><div className="page-shell relative flex min-h-[78svh] flex-col justify-end pb-20 md:pb-28"><p className="eyebrow text-bone/70">{eyebrow}</p><h1 className="display max-w-5xl text-[clamp(4rem,10vw,9rem)]">{title}</h1><p className="mt-8 max-w-xl text-lg text-bone/80">{copy}</p></div></section>}
