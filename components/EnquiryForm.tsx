'use client'
import {useSearchParams} from 'next/navigation'
import {useState} from 'react'
const intents=['Enquire About a Residence','Request Floor Plans','Schedule a Viewing','Talk to Sales']
export function EnquiryForm(){const params=useSearchParams();const initial=params.get('residence')?`I would like to know more about the ${params.get('residence')?.replaceAll('-',' ')} residence.`:'';const[msg,setMsg]=useState(initial);const[sent,setSent]=useState(false);if(sent)return <div className="border-l border-bronze pl-8 py-10"><p className="eyebrow">THANK YOU</p><h2 className="font-display text-5xl">Your interest has been noted.</h2><p className="mt-5 max-w-lg text-charcoal/60">This prototype is ready to connect to the confirmed ONA sales channel.</p></div>;return <><div className="mb-12 flex flex-wrap gap-2">{intents.map(x=><button type="button" onClick={()=>setMsg(`${x}. `)} className="border border-charcoal/25 px-4 py-2 text-xs hover:border-bronze" key={x}>{x}</button>)}</div><form onSubmit={e=>{e.preventDefault();setSent(true)}} className="space-y-8"><Field label="Name"><input name="name" required autoComplete="name"/></Field><div className="grid gap-8 md:grid-cols-2"><Field label="Phone / WhatsApp"><input name="phone" required autoComplete="tel"/></Field><Field label="Email"><input name="email" type="email" required autoComplete="email"/></Field></div><Field label="Apartment interest"><select name="interest" defaultValue=""><option value="" disabled>Select one</option><option>2BR</option><option>3BR</option><option>Penthouse</option><option>Not sure yet</option></select></Field><Field label="Message"><textarea name="message" rows={4} value={msg} onChange={e=>setMsg(e.target.value)}/></Field><button className="bg-charcoal px-7 py-4 text-xs font-medium text-ivory" type="submit">Send enquiry</button></form></>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block text-[10px] font-medium uppercase tracking-[.18em]">{label}<div className="mt-2 [&>*]:w-full [&>*]:border-0 [&>*]:border-b [&>*]:border-charcoal/30 [&>*]:bg-transparent [&>*]:py-3 [&>*]:outline-none">{children}</div></label>}
